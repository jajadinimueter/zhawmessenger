package zhawmessenger.messagesystem.api.transport;

/**
 * Extend from {@link RuntimeException} because we
 * don't want to force users to catch it.
 */
public class TransportException extends RuntimeException {
}
